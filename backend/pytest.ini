[pytest]
testpaths = tests
addopts = -q
filterwarnings =
    # Third-party: ezdxf calls deprecated pyparsing APIs when reading a DXF.
    # Out of our control; silence only this one so our own warnings stay visible.
    ignore::pyparsing.warnings.PyparsingDeprecationWarning
